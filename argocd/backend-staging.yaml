apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: helioops-backend-staging
  namespace: argocd
  labels:
    app: helioops
    component: backend
    env: staging
spec:
  project: default
  source:
    repoURL: https://github.com/trh-ds/HelioOps
    targetRevision: HEAD
    path: k8s/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true