apiVersion: v1
kind: Service
metadata:
  name: helioops-backend
  labels:
    app: helioops
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: helioops
    component: backend