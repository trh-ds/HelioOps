apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: helioops-backend
  labels:
    app: helioops
    component: backend
spec:
  selector:
    matchLabels:
      app: helioops
      component: backend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s