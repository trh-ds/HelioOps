# HelioOps backend — serving dependencies only.
# Offline work (PDF ingest, FITS download, model training) lives in requirements-dev.txt
# so it never lands in the deployed image.

# API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Config & logging
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
structlog>=24.1.0

# HTTP
httpx>=0.27.0          # Supabase repository
requests>=2.31.0       # DONKI / GOES XRS / DSCOVR clients

# LLM (layer 4)
groq>=0.9.0
tiktoken>=0.7.0        # prompt token budgeting

# RAG (layer 4)
chromadb>=0.5.0
sentence-transformers>=3.0.0

# ML inference (layer 3)
numpy>=1.26.0
pandas>=2.2.0
joblib>=1.4.0
lightgbm>=4.3.0

# CV (layer 1)
opencv-python-headless>=4.9   # headless: no GUI libs in the image
astropy>=6.0                  # FITS reading
