CHECKLIST: What We Need For CI/CD To Fully Pass
==================================================

Share this with your teammates. Anything marked [MISSING] needs to be added
to the repo (git-tracked or provided via shared storage).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. GROQ_API_KEY
   [MISSING - NEEDED FROM PRIYANSHU]
   - Get from https://console.groq.com/keys (free tier works)
   - Add as GitHub Secret: repo Settings → Secrets → GROQ_API_KEY
   - Also add locally: echo "GROQ_API_KEY=gsk_real_key" > .env
   - Used by genai/ (agents call Groq Llama 3.3 70B)

2. ML Model Checkpoints
   [MISSING - NEEDED FROM NEAL]
   - 6 .pkl files in ML_after_CV/checkpoints/:
     - gps_q025.pkl, gps_q500.pkl, gps_q975.pkl
     - hf_q025.pkl, hf_q500.pkl, hf_q975.pkl
   - Generated by running: python ML_after_CV/02_train_and_tune.py
   - Or ask Neal to share the 6 files
   - Without these, ML prediction falls back to hardcoded defaults (20m GPS, 85% HF)

3. ChromaDB Vector Database
   [MISSING - NEEDED FROM PRIYANSHU]
   - Full directory: data/chroma_db/
   - Contains 5 collections (aviation_kb, grid_kb, maritime_kb, telecom_kb, impact_matrix_kb)
   - Generated by running: python -m embeddings.ingest_aviation (and grid, maritime, etc.)
   - Or ask Priyanshu to share the data/chroma_db/ folder
   - Without these, RAG retrieval fails and GenAI agents can't cite sources

4. Cached CV Data (FITS + PNGs)
   [MISSING - NEEDED FROM NEAL]
   - These directories are gitignored (too large) but needed for detection:
     - data/cached/ccor1/2024-10/ (preprocessed PNGs + annotated/)
     - data/cached/lasco/2024-05/ (preprocessed PNGs + annotated/)
     - data/cached/donki/ (JSON files from NASA DONKI API)
     - data/cached/xrs/ (JSON files from GOES XRS)
     - data/cached/l1/ (JSON files from DSCOVR L1 data)
   - Generated by running: python -m cv.cache_fits
   - Without these, detect() falls back to stub JSON (still works but no real images)

5. PDF Source Documents (for RAG ingestion)
   [EXISTS - but need ChromaDB built from them]
   - data/aviation/nat_doc_007_2025.pdf ✓
   - data/grid/nerc_tpl007_4.pdf ✓ (plus 3 more)
   - data/maritime/imo_gmdss_2019.pdf ✓
   - data/impact_matrix/noaa_space_weather_scales.txt ✓
   - These PDFs exist but haven't been ingested into ChromaDB yet
   - Once you have ChromaDB (#3 above), this is done automatically

6. GitHub Repository Settings
   [ACTION ITEM - DO THIS ON GITHUB]
   - Add these as Repository Secrets (Settings → Secrets and variables → Actions):
     - GROQ_API_KEY = gsk_your_actual_key
   - These ensure CI can run tests without hardcoding keys

7. Docker Registry Access
   [ACTION ITEM - FOR DEPLOYMENT]
   - Create a GitHub Container Registry (GHCR) token
   - Add as GitHub Secret: CR_PAT (for pushing Docker images)
   - Only needed when you move to CD (continuous deployment), not for CI

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MINIMUM VIABLE TO MAKE CI GREEN:
  - Items 1 (GROQ_API_KEY as GitHub Secret)
  - Item 6 (add GitHub Secret)
  Everything else has fallbacks. CI is configured with || true
  so it passes even without data, but tests will be shallow.

FOR FULL END-TO-END:
  - All items 1-6 needed
  - Item 7 only when deploying to cloud

TEAMMATE RESPONSIBILITIES:
  - NEAL: Items 2 (ML checkpoints), 4 (cached CV data)
  - PRIYANSHU: Items 1 (confirm API key), 3 (ChromaDB data)
  - TIRTH: Items 6-7 (GitHub settings), verify CI passes